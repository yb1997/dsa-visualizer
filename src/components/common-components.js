import styled from "styled-components";

export const ArrayInput = styled.input`
  padding: 0.4em 0.7em;
  box-shadow: 0 2px 8px #ddd;
  border: none;
  font-size: 2em;
  margin-top: 1em;
`;

export const Output = styled.code`
  padding: 0.5em;
  background: #333;
  color: #ddd;
  width: 50vw;
  display: inline-block;
`;

export const Center = styled.div`
  text-align: center;
`;

export const Button = styled.button`
  background: transparent;
  border-radius: 3px;
  border: 2px solid palevioletred;
  color: palevioletred;
  margin: 0 1em;
  padding: 0.25em 1em;
  font-size: 2em;
  cursor: pointer;
  box-shadow: 0 2px 8px #ddd;
`;